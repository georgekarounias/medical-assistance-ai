{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5433;Database=ai_med;Username=postgres;Password=postgres"
  },
  "Authentication": {
    "//": "Local development only. Real secrets come from the estate's secret store; keep two entries here while rotating.",
    "ApiKeys": [ "local-development-key" ]
  }
}
